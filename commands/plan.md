---
description: 明确需求，评估风险，并创建分步实施计划。在触碰任何代码之前等待用户确认。
---

# Plan 命令

此命令调用 **planner** agent在写任何代码之前创建一个全面的实施计划。

## 此命令做什么

1. **明确需求** - 澄清需要构建什么
2. **识别风险** - 发现潜在问题和障碍
3. **创建分步计划** - 将实施分解为阶段
4. **等待确认** - 在继续之前必须获得用户批准

## 何时使用

使用 `/plan` 当：
- 开始新功能
- 进行重大架构变更
- 进行复杂重构
- 多个文件/组件将受影响
- 需求不清晰或模糊

## 工作原理

planner agent将：

1. **分析请求** 并用清晰术语明确需求
2. **分解为阶段**，包含具体、可执行的步骤
3. **识别组件之间的依赖**
4. **评估风险** 和潜在障碍
5. **估计复杂度**（高/中/低）
6. **展示计划** 并等待你的明确确认

## 示例用法

```
用户: /plan 我需要在市场结算时添加实时通知

agent (planner):
# 实施计划：市场结算实时通知

## 需求重述
- 当用户关注的市场结算时发送通知
- 支持多种通知渠道（应用内、邮件、webhook）
- 确保通知可靠送达
- 包含市场结果和用户的仓位结果

## 实施阶段

### 阶段 1：数据库模式
- 添加 notifications 表，包含列：id、user_id、market_id、type、status、created_at
- 添加 user_notification_preferences 表用于渠道偏好
- 在 user_id 和 market_id 上创建索引以提高性能

### 阶段 2：通知服务
- 在 lib/notifications.ts 中创建通知服务
- 使用 BullMQ/Redis 实现通知队列
- 为失败的投递添加重试逻辑
- 创建通知模板

### 阶段 3：集成点
- 挂入市场结算逻辑（当状态变为"已结算"时）
- 查询市场中所有有仓位的用户
- 为每个用户入队通知

### 阶段 4：前端组件
- 在头部创建 NotificationBell 组件
- 添加 NotificationList 模态框
- 通过 Supabase 订阅实现实时更新
- 添加通知偏好设置页面

## 依赖
- Redis（用于队列）
- 邮件服务（SendGrid/Resend）
- Supabase 实时订阅

## 风险
- 高：邮件送达能力（需要 SPF/DKIM）
- 中：每个市场 1000+ 用户时的性能
- 中：如果市场频繁结算，通知垃圾
- 低：实时订阅开销

## 估计复杂度：中
- 后端：4-6 小时
- 前端：3-4 小时
- 测试：2-3 小时
- 总计：9-13 小时

**等待确认**：继续此计划？（是/否/修改）
```

## 重要提示

**关键**：planner agent在用户用"是"或"继续"或类似肯定响应明确确认计划之前，**不会**写任何代码。

如果你想要变更，回复：
- "修改：[你的变更]"
- "不同方法：[替代方案]"
- "跳过阶段 2 先做阶段 3"

## 与其他命令集成

规划后：
- 使用 `/tdd` 通过测试驱动开发实现
- 如有构建错误使用 `/build-fix`
- 使用 `/code-review` 审查完成的实现

## 相关agent

此命令调用 something-claude-code 提供的 `planner` agent。

手动安装时，源文件位于：
`agents/planner.md`
