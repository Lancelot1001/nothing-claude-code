# 模型路由命令

根据复杂度和预算为当前任务推荐最佳模型层级。

## 使用方法

`/model-route [任务描述] [--budget low|med|high]`

## 路由启发式

- `haiku`：确定性、低风险的机械更改
- `sonnet`：实现和重构的默认选择
- `opus`：架构、深度审查、模糊需求

## 必需输出

- 推荐的模型
- 置信度级别
- 为什么这个模型适合
- 第一次尝试失败时的后备模型

## 参数

$ARGUMENTS：
- `[任务描述]` 可选自由文本
- `--budget low|med|high` 可选
